import 'package:flutter/material.dart';

class EpubView extends StatelessWidget {
  const EpubView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
